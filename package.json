{
  "name": "smartdev",
  "version": "0.1.0",
  "private": true,
  "description": "Voice Dev Assistant — talk to Claude while connected to your workspace",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:bridge": "npm run dev -w apps/local-bridge",
    "dev:orchestrator": "npm run dev -w apps/orchestrator",
    "test": "node --test tests/*.test.ts",
    "build": "npm run build -w apps/local-bridge && npm run build -w apps/orchestrator"
  }
}
